package com.msaggik.playlistmaker.player.domain.state

enum class PlayerState {
    PLAYER_STATE_DEFAULT,
    PLAYER_STATE_PREPARED,
    PLAYER_STATE_PLAYING,
    PLAYER_STATE_PAUSED,
    PLAYER_STATE_STOP,
}